# TapLog Core

This module contains Android-independent domain values, historical Event models,
validated channel-neutral commands, and the transaction-facing Event Engine
boundary. Android UI, channel dispatch and duplicate handling, Room/SQLite,
network, parser/AI, backup, and post-commit feedback belong to outer modules.
